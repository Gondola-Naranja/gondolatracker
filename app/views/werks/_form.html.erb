<%= form_for(@werk) do |f| %>
  <% if @werk.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@werk.errors.count, "error") %> prohibited this werk from being saved:</h2>

      <ul>
      <% @werk.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
