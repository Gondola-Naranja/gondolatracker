<p id="notice"><%= notice %></p>

<h1>Listing Werks</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @werks.each do |werk| %>
      <tr>
        <td><%= werk.name %></td>
        <td><%= link_to 'Show', werk %></td>
        <td><%= link_to 'Edit', edit_werk_path(werk) %></td>
        <td><%= link_to 'Destroy', werk, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Werk', new_werk_path %>
